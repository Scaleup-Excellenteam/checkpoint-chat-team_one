version: '3.8'

services:
  frontend:
    image: node:14
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: npm start

  backend:
    image: mongo
    ports:
      - "27017:27017"